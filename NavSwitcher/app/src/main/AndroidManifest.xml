<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 悬浮窗 + 前台通知权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Android 14+ 前台服务类型必须对齐：这里我们用 dataSync（和 Service 上的 foregroundServiceType 一致） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- 建议显式声明 Shizuku 权限（库会自动合并一份，但手动写更稳，防止某些合并配置丢失） -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:supportsRtl="true"
        tools:ignore="UnusedAttribute">

        <!-- Shizuku Provider（必须，authorities 用 ${applicationId}.shizuku） -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:exported="true"
            tools:ignore="ExportedProvider" />

        <!-- 被 Shizuku 绑定的用户服务。注意：不要手动指定 android:process，Shizuku 会按你传的 suffix 创建进程 -->
        <service
            android:name=".ShellUserService"
            android:exported="false"
            android:stopWithTask="false" />

        <!-- 悬浮球前台服务：与上面的 uses-permission 对齐为 dataSync -->
        <service
            android:name=".FloatService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
