pluginManagement {
  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
    maven { url "https://maven.rikka.dev" }   // ★ 新增：Shizuku 官方仓库
  }
}
dependencyResolutionManagement {
  repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
  repositories {
    google()
    mavenCentral()
    maven { url "https://maven.rikka.dev" }   // ★ 新增
  }
}
rootProject.name = "NavSwitcher"
include(":app")
